import React from 'react'
import ReactDOM from 'react-dom/client'
import './styles/index.css'

// TodoApp will be imported and used once implemented
console.log('Todo List App - Entry point ready')

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Todo App will be rendered here</div>
  </React.StrictMode>,
)